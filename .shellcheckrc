# Scripts here source siblings at runtime paths shellcheck cannot resolve
# statically, and several functions are reached only through a trap or from a
# case script that sources the library — both read as "unreachable" to the
# static checker.
disable=SC1091,SC2317,SC2329
